import React from "react";
import { StyledModalButton } from "./styles/StyledModalButton";

const ViewControlButton = () => {
  return (
    <div>
      <StyledModalButton>Controls</StyledModalButton>
    </div>
  );
};

export default ViewControlButton;
